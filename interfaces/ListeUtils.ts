// permet de d'appeler la méthode getId() dans des listes de différentes natures,
// pour comparer des éléments grâce à leurs id
export interface ListeUtils {

    getId(): string;
    description();

}